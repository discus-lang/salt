

proc fac [n: #Nat]: #Nat
 = if   { #nat'eq [n, 0] → 4
        ; otherwise      → #nat'mul [n, fac [#nat'sub [n, 1]]] }


-- proc derp [n: #Nat]: #Nat
--  = let  x: #Nat = 4;
--    seq  when { #nat'eq [3, 4] → return 4 }
--         when { #nat'eq [5, 6] → [] }
--         let x = #nat'add [4, 5];
--         case n of { bar [] → return 5 }
--         cell n: #Nat ← 0;
--         loop seq
--                 when { #nat'eq [n, 0] → break }
--                 n ← 4;
--                 #console'print "bar";
--                 #console'print "baz";
--         end
--         #console'print "qux"
--    end

